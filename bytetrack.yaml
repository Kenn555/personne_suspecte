# bytetrack.yaml - default Ultralytics tracker config

tracker_type: bytetrack
track_high_thresh: 0.5
track_low_thresh: 0.1
new_track_thresh: 0.6
track_buffer: 30
match_thresh: 0.8
frame_rate: 30
mot20: False

# ByteTrack specific parameters
obj_score_thresh: 0.5
min_box_area: 10
